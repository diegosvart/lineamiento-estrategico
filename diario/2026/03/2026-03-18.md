---
aliases:
  - Diario 18-03-2026
tags:
  - diario
fecha: 2026-03-18
semana: 12
entradas:
  - proyecto: Plan Gobernanza TI
    iniciativa: HUB de Integración
    rol: Arquitecto / Desarrollador de Software
    tipo-trabajo: Ejecución Operativa
    actividad: Documentación técnica
    modalidad: Remoto
    horas: 1
    descripcion: Desarrollo de imágenes y material explicativo del modelo ERP por base de datos
    estado: Completado
  - proyecto: Plan Gobernanza TI
    iniciativa: HUB de Integración
    rol: Consultor de Gobernanza TI
    tipo-trabajo: Planificación y Diseño
    actividad: Diagnóstico del estado actual
    modalidad: Remoto
    horas: 1
    descripcion: Identificación de prioridades, alcance técnico y objetivos del HUB de integración
    estado: Completado
---

# Diario 18-03-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
