---
aliases:
  - Diario 06-03-2026
tags:
  - diario
fecha: 2026-03-06
semana: 10
entradas:
  - proyecto: Cash Flow
    rol: Project Manager
    tipo-trabajo: Gestión
    actividad: Levantamiento de información
    modalidad: Remoto
    horas: 0.5
    descripcion: Revisión archivos Cash Flow — análisis de archivos existentes del proceso de flujo de caja
    estado: Completado
  - proyecto: Plan Gobernanza TI
    iniciativa: Gestión del Plan
    rol: Project Manager
    tipo-trabajo: Gestión
    actividad: Seguimiento y control
    modalidad: Remoto
    horas: 0.5
    descripcion: Solicitud apoyo SIG #477 — atención y seguimiento de solicitud de soporte al área TI
    estado: Completado
---

# Diario 06-03-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
