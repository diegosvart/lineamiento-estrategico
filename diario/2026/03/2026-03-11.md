---
aliases:
  - Diario 11-03-2026
tags:
  - diario
fecha: 2026-03-11
semana: 11
entradas:
  - proyecto: Cash Flow
    rol: Project Manager
    tipo-trabajo: Gestión
    actividad: Seguimiento y control
    modalidad: Remoto
    horas: 2
    descripcion: Seguimiento funcional con proveedor TMQ; pendiente envío de definiciones técnicas y funcionales para evaluar brechas y esfuerzo de desarrollo
    estado: En curso
  - proyecto: Seguros & Siniestros
    rol: Project Manager
    tipo-trabajo: Ejecución Operativa
    actividad: Documentación de fichas y procesos
    modalidad: Remoto
    horas: 2
    descripcion: Revisión y ajustes a la ficha inicial del proyecto Seguros y Siniestros
    estado: Completado
  - proyecto: Plan Gobernanza TI
    iniciativa: Diagnóstico Normativo
    rol: Analista de Seguridad
    tipo-trabajo: Ejecución Operativa
    actividad: Identificación de brechas
    modalidad: Remoto
    horas: 2
    descripcion: Creación de ticket GLPI por brecha de seguridad (acceso sin restricciones a recursos compartidos en IP interna)
    estado: Completado
---

# Diario 11-03-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
