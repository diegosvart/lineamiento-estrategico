---
aliases:
  - Diario 24-03-2026
tags:
  - diario
fecha: 2026-03-24
semana: 13
entradas:
  - proyecto: Plan Gobernanza TI
    iniciativa: Gestión del Plan
    rol: Project Manager
    tipo-trabajo: Gestión
    actividad: Seguimiento y control
    modalidad: Remoto
    horas: 1
    descripcion: Revisión de tareas del PM — revisión y coordinación de tareas y avances con jefatura TI
    estado: Completado
  - proyecto: Plan Gobernanza TI
    iniciativa: Gestión del Plan
    rol: Project Manager
    tipo-trabajo: Planificación y Diseño
    actividad: Documentación de fichas y procesos
    modalidad: Remoto
    horas: 2
    descripcion: Coordinación y cierre de ficha de proyecto SIA — definición de alcance, estructura final y revisión con equipo
    estado: Completado
  - proyecto: Seguros & Siniestros
    rol: Project Manager
    tipo-trabajo: Planificación y Diseño
    actividad: Documentación de fichas y procesos
    modalidad: Remoto
    horas: 2
    descripcion: Revisión cruzada de ficha de proyecto Seguros/Siniestros con líderes de área; validación de alcance y levantamiento de observaciones
    estado: Completado
---

# Diario 24-03-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
