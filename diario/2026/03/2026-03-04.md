---
aliases:
  - Diario 04-03-2026
tags:
  - diario
fecha: 2026-03-04
semana: 10
entradas:
  - proyecto: Plan Gobernanza TI
    iniciativa: Diagnóstico Normativo
    rol: Analista de Seguridad
    tipo-trabajo: Ejecución Operativa
    actividad: Identificación de brechas
    modalidad: Remoto
    horas: 2
    descripcion: Análisis de requisitos para seguros de ciberseguridad; identificación de brecha MFA que impide cobertura efectiva; planteamiento de necesidad de rol interno de infraestructura y seguridad
    estado: Completado
  - proyecto: Cash Flow
    rol: Project Manager
    tipo-trabajo: Planificación y Diseño
    actividad: Levantamiento de información
    modalidad: Remoto
    horas: 2
    descripcion: Levantamiento funcional de procesos de cash flow; solicitud de campos asociados a procesos manuales; recepción de documentación Excel y archivos compartidos
    estado: En curso
---

# Diario 04-03-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
