---
aliases:
  - Diario 02-03-2026
tags:
  - diario
fecha: 2026-03-02
semana: 10
entradas:
  - proyecto: Plan Gobernanza TI
    iniciativa: Gestión del Plan
    rol: Project Manager
    tipo-trabajo: Gestión
    actividad: Seguimiento y control
    modalidad: Remoto
    horas: 1
    descripcion: Planilla de proyectos — revisión y actualización del estado de iniciativas en portafolio
    estado: Completado
  - proyecto: Cash Flow
    rol: Project Manager
    tipo-trabajo: Planificación y Diseño
    actividad: Levantamiento de información
    modalidad: Remoto
    horas: 1
    descripcion: Levantamiento funcional Cash Flow — relevamiento de requerimientos funcionales del proceso
    estado: Completado
  - proyecto: Plan Gobernanza TI
    iniciativa: Gestión del Plan
    rol: Project Manager
    tipo-trabajo: Gestión
    actividad: Presentaciones a Gerencia
    modalidad: Remoto
    horas: 1
    descripcion: Presentación metodología gestión — exposición de metodología de gestión de proyectos a equipo TI
    estado: Completado
---

# Diario 02-03-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
