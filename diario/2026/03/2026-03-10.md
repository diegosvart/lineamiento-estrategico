---
aliases:
  - Diario 10-03-2026
tags:
  - diario
fecha: 2026-03-10
semana: 11
entradas:
  - proyecto: Seguros & Siniestros
    rol: Project Manager
    tipo-trabajo: Planificación y Diseño
    actividad: Diseño de flujos de gobernanza
    modalidad: Remoto
    horas: 2
    descripcion: Inicio formal del proyecto Seguros y Siniestros; designación de responsables; solicitud y envío de ficha de proyecto
    estado: Completado
  - proyecto: Gestión de documentos
    rol: Project Manager
    tipo-trabajo: Planificación y Diseño
    actividad: Diseño de flujos de gobernanza
    modalidad: Remoto
    horas: 2
    descripcion: Invitación formal al inicio del proyecto Gestión Documental; coordinación de reunión para completar ficha de proyecto
    estado: Completado
  - proyecto: Plan Gobernanza TI
    iniciativa: Catastro de Aplicaciones
    rol: DBA / Analista de Infraestructura
    tipo-trabajo: Planificación y Diseño
    actividad: Levantamiento de instancias y sistemas
    modalidad: Remoto
    horas: 2
    descripcion: Solicitud y habilitación de acceso de solo lectura a bases de datos del holding
    estado: Completado
---

# Diario 10-03-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
