---
aliases:
  - Diario 05-03-2026
tags:
  - diario
fecha: 2026-03-05
semana: 10
entradas:
  - proyecto: Plan Gobernanza TI
    iniciativa: Gestión del Plan
    rol: Project Manager
    tipo-trabajo: Gestión
    actividad: Presentaciones a Gerencia
    modalidad: Remoto
    horas: 0.5
    descripcion: Revisión presentación planificación — ajustes previos a presentación formal de planificación
    estado: Completado
  - proyecto: Cash Flow
    rol: Project Manager
    tipo-trabajo: Gestión
    actividad: Seguimiento y control
    modalidad: Remoto
    horas: 2
    descripcion: Revisión de API Key Xpendit, validación de acceso multiempresa y seguimiento de riesgos funcionales del proyecto
    estado: En curso
  - proyecto: Plan Gobernanza TI
    iniciativa: Catastro de Aplicaciones
    rol: DBA / Analista de Infraestructura
    tipo-trabajo: Planificación y Diseño
    actividad: Levantamiento de instancias y sistemas
    modalidad: Remoto
    horas: 2
    descripcion: Evaluación de Starlink para equipos móviles; análisis de cobertura satelital, geolocalización y viabilidad como solución corporativa para operaciones sin señal celular
    estado: Completado
---

# Diario 05-03-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
