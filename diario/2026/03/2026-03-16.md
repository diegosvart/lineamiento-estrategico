---
aliases:
  - Diario 16-03-2026
tags:
  - diario
fecha: 2026-03-16
semana: 12
entradas:
  - proyecto: Plan Gobernanza TI
    iniciativa: HUB de Integración
    rol: Arquitecto / Desarrollador de Software
    tipo-trabajo: Ejecución Operativa
    actividad: Documentación técnica
    modalidad: Remoto
    horas: 1
    descripcion: Modificación de especificación técnica v2 y adaptación a condiciones actuales
    estado: Completado
  - proyecto: Plan Gobernanza TI
    iniciativa: Gestión del Plan
    rol: Project Manager
    tipo-trabajo: Gestión
    actividad: Seguimiento y control
    modalidad: Remoto
    horas: 1
    descripcion: Elaboración de informe automático por plan para reportería de trabajo y proyectos
    estado: Completado
  - proyecto: Plan Gobernanza TI
    iniciativa: Automatización Entorno Digital
    rol: Arquitecto / Desarrollador de Software
    tipo-trabajo: Ingeniería y Desarrollo
    actividad: Integración con APIs
    modalidad: Remoto
    horas: 1
    descripcion: Trabajo en visibilidad de planes y tareas a través de Microsoft Graph
    estado: Completado
---

# Diario 16-03-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
