---
aliases:
  - Diario 31-03-2026
tags:
  - diario
fecha: 2026-03-31
semana: 14
entradas:
  - proyecto: Plan Gobernanza TI
    iniciativa: Gestión del Plan
    rol: Consultor de Gobernanza TI
    tipo-trabajo: Planificación y Diseño
    actividad: Diagnóstico del estado actual
    modalidad: Presencial
    horas: 1
    descripcion: "GLPI: coordinación de reunión para levantar funcionalidad de inventario de aplicaciones"
    estado: En curso
  - proyecto: Sitrack
    rol: Project Manager
    tipo-trabajo: Planificación y Diseño
    actividad: Levantamiento de información
    modalidad: Presencial
    horas: 1
    descripcion: "Sitrack-COPEC: formalización de nuevo requerimiento de integración y discusión de riesgo de dependencia externa"
    estado: En curso
---

# Diario 31-03-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
