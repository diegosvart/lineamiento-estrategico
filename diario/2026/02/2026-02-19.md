---
aliases:
  - Diario 19-02-2026
tags:
  - diario
fecha: 2026-02-19
semana: 8
entradas:
  - proyecto: Plan Gobernanza TI
    iniciativa: Automatización Entorno Digital
    rol: Consultor de Gobernanza TI
    tipo-trabajo: Planificación y Diseño
    actividad: Diagnóstico del estado actual
    modalidad: Remoto
    horas: 1
    descripcion: Entrevista de diagnóstico técnico M365 — licencias, accesos, escala y estado del script
    estado: Completado
  - proyecto: Plan Gobernanza TI
    iniciativa: Automatización Entorno Digital
    rol: Arquitecto / Desarrollador de Software
    tipo-trabajo: Planificación y Diseño
    actividad: Diseño de soluciones técnicas
    modalidad: Remoto
    horas: 1
    descripcion: Diseño arquitectura entorno M365 — Grupo, Teams, canales por proyecto, Planner y To Do
    estado: Completado
---

# Diario 19-02-2026

> Registro de actividades y horas del día. Generado via `/vault-timesheet`.

## Entradas del día

```dataviewjs
const entradas = dv.current().file.frontmatter.entradas || [];
if (entradas.length === 0) {
  dv.paragraph("> Sin entradas registradas.");
} else {
  const rows = [].concat(entradas).map(e => [
    e?.proyecto ?? "—",
    e?.iniciativa ?? "—",
    e?.rol ?? "—",
    e?.["tipo-trabajo"] ?? "—",
    e?.actividad ?? "—",
    e?.horas ?? 0,
    e?.estado ?? "—"
  ]);
  dv.table(["Proyecto", "Iniciativa", "Rol", "Tipo Trabajo", "Actividad", "Horas", "Estado"], rows);
  const total = [].concat(entradas).reduce((s, e) => s + (e?.horas ?? 0), 0);
  dv.paragraph(`**Total horas:** ${total}`);
}
```
